import { Contact } from 'src/sections/contact';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Contact: ',
};

export default function ContactPage() {
  return <Contact />;
}
